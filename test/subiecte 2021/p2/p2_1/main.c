#include "lib.h"

int main(void) {

  int size = 6;
  char *array[6] = {"se", "cumsecade", "cade", "cum", "adusese", "adu"};

  // print_string_array(stdout, array, size);

  back(array, size);

  return 0;
}
